fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
boto3>=1.34.0
numpy>=2.0
scipy>=1.13
# Optional: for Vanna text-to-SQL (OpenAI/Anthropic instead of Bedrock)
# vanna chromadb openai   # full Vanna + OpenAI + ChromaDB
# openai                  # fallback: OpenAI-only SQL generation
